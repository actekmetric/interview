name: Observability Stack CD

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'sre/helm/observability/**'
      - 'sre/terraform/modules/amp/**'
      - 'sre/terraform/modules/grafana/**'
      - '.github/workflows/sre-observability-cd.yml'

  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - dev
          - qa
          - prod
        default: dev
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - deploy
          - upgrade
          - uninstall
        default: deploy

# Prevent concurrent deployments to the same environment
concurrency:
  group: observability-cd-${{ github.event.inputs.environment || 'auto' }}
  cancel-in-progress: false

# Required for OIDC authentication with AWS
permissions:
  id-token: write   # Required for AWS OIDC authentication
  contents: read    # Required to checkout repository

jobs:
  # Determine which environments to deploy to
  determine-environments:
    name: Determine Deployment Targets
    runs-on: ubuntu-latest
    outputs:
      environments: ${{ steps.determine.outputs.environments }}
    steps:
      - name: Determine environments
        id: determine
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            # Manual: deploy only to specified environment
            echo "environments=[\"${{ inputs.environment }}\"]" >> $GITHUB_OUTPUT
          else
            # Automatic: deploy to dev only (safety)
            echo "environments=[\"dev\"]" >> $GITHUB_OUTPUT
          fi

  # Deploy to DEV
  deploy-dev:
    name: Deploy to DEV
    runs-on: ubuntu-latest
    needs: determine-environments
    if: contains(fromJson(needs.determine-environments.outputs.environments), 'dev')
    environment:
      name: dev
      url: https://g-dev-grafana.grafana-workspace.us-east-1.amazonaws.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform and Terragrunt
        uses: ./.github/actions/terraform-setup

      - name: Assume AWS Role
        uses: ./.github/actions/aws-assume-role
        with:
          environment: dev
          role-arn: ${{ secrets.AWS_DEV_ROLE_ARN }}
          account-id: ${{ secrets.AWS_DEV_ACCOUNT_ID }}

      - name: Get Terraform Outputs
        id: terraform-outputs
        run: |
          cd sre/terragrunt/environments/dev/observability/amp

          # Get AMP workspace endpoint
          AMP_ENDPOINT=$(terragrunt output -raw workspace_endpoint 2>/dev/null || echo "")
          if [[ -z "$AMP_ENDPOINT" ]]; then
            echo "::error::Failed to get AMP workspace endpoint. Ensure AMP is deployed."
            exit 1
          fi

          # Get Prometheus Agent IAM role ARN
          ROLE_ARN=$(terragrunt output -raw prometheus_agent_role_arn 2>/dev/null || echo "")
          if [[ -z "$ROLE_ARN" ]]; then
            echo "::error::Failed to get Prometheus Agent IAM role ARN. Ensure AMP is deployed."
            exit 1
          fi

          echo "amp-endpoint=${AMP_ENDPOINT}" >> $GITHUB_OUTPUT
          echo "role-arn=${ROLE_ARN}" >> $GITHUB_OUTPUT

          echo "📊 AMP Endpoint: ${AMP_ENDPOINT}"
          echo "🔐 IAM Role ARN: ${ROLE_ARN}"

      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig \
            --name tekmetric-dev \
            --region us-east-1

      - name: Prepare Helm Values
        run: |
          cd sre/helm/observability

          # Create temporary values file with Terraform outputs
          cat > values-dev-generated.yaml << EOF
          # Auto-generated values from Terraform outputs
          # Base values are imported from values-dev.yaml

          global:
            environment: dev
            region: us-east-1

          prometheusAgent:
            enabled: true
            replicas: 1

            serviceAccount:
              create: true
              name: prometheus-agent
              annotations:
                eks.amazonaws.com/role-arn: "${{ steps.terraform-outputs.outputs.role-arn }}"

            amp:
              remoteWriteUrl: "${{ steps.terraform-outputs.outputs.amp-endpoint }}"
              sigv4:
                enabled: true
                region: us-east-1

            scrapeInterval: 30s
            scrapeTimeout: 10s

            resources:
              requests:
                memory: "256Mi"
                cpu: "100m"
              limits:
                memory: "512Mi"
                cpu: "250m"

          kubeStateMetrics:
            enabled: true
            resources:
              requests:
                memory: "64Mi"
                cpu: "50m"
              limits:
                memory: "128Mi"
                cpu: "100m"

          nodeExporter:
            enabled: true
            resources:
              requests:
                memory: "32Mi"
                cpu: "25m"
              limits:
                memory: "64Mi"
                cpu: "50m"
          EOF

          echo "✅ Generated values file with Terraform outputs"
          cat values-dev-generated.yaml

      - name: Ensure Namespace Exists
        if: github.event.inputs.action != 'uninstall'
        run: |
          kubectl create namespace observability --dry-run=client -o yaml | kubectl apply -f -
          echo "✅ Namespace 'observability' is ready"

      - name: Deploy/Upgrade Observability Stack
        if: github.event.inputs.action != 'uninstall'
        run: |
          cd sre/helm/observability

          helm upgrade --install observability . \
            --namespace observability \
            --values values-dev-generated.yaml \
            --wait \
            --timeout 10m

          echo "✅ Helm deployment completed"

      - name: Uninstall Observability Stack
        if: github.event.inputs.action == 'uninstall'
        run: |
          helm uninstall observability --namespace observability --wait || true
          echo "🗑️ Observability stack uninstalled"

      - name: Verify Deployment
        if: github.event.inputs.action != 'uninstall'
        run: |
          echo "📋 Checking pod status..."
          kubectl get pods -n observability

          echo ""
          echo "⏳ Waiting for pods to be ready..."
          kubectl wait --for=condition=ready pod \
            --all \
            --namespace observability \
            --timeout=300s

          echo ""
          echo "✅ All pods are ready!"

          echo ""
          echo "📊 Checking Prometheus Agent logs..."
          kubectl logs -n observability deployment/prometheus-agent --tail=20 || true

      - name: Generate Deployment Summary
        if: always() && github.event.inputs.action != 'uninstall'
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 📊 Observability Stack Deployment - DEV

          **Environment:** dev
          **Action:** ${{ github.event.inputs.action || 'deploy' }}
          **Status:** ${{ job.status }}

          ### Components Deployed
          - ✅ Prometheus Agent (scrapes metrics, sends to AMP)
          - ✅ kube-state-metrics (cluster metrics)
          - ✅ node-exporter (node metrics)

          ### Configuration
          - **AMP Endpoint:** `${{ steps.terraform-outputs.outputs.amp-endpoint }}`
          - **IRSA Role:** `${{ steps.terraform-outputs.outputs.role-arn }}`
          - **Namespace:** `observability`

          ### Quick Commands
          ```bash
          # View pods
          kubectl get pods -n observability

          # View Prometheus Agent logs
          kubectl logs -n observability deployment/prometheus-agent -f

          # Port-forward to Prometheus
          kubectl port-forward -n observability deployment/prometheus-agent 9090:9090

          # Check metrics in Grafana
          # Login to AMG and query: up{job="kubernetes-pods"}
          ```
          EOF

  # Deploy to QA
  deploy-qa:
    name: Deploy to QA
    runs-on: ubuntu-latest
    needs: determine-environments
    if: contains(fromJson(needs.determine-environments.outputs.environments), 'qa')
    environment:
      name: qa
      url: https://g-qa-grafana.grafana-workspace.us-east-1.amazonaws.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform and Terragrunt
        uses: ./.github/actions/terraform-setup

      - name: Assume AWS Role
        uses: ./.github/actions/aws-assume-role
        with:
          environment: qa
          role-arn: ${{ secrets.AWS_QA_ROLE_ARN }}
          account-id: ${{ secrets.AWS_QA_ACCOUNT_ID }}

      - name: Get Terraform Outputs
        id: terraform-outputs
        run: |
          cd sre/terragrunt/environments/qa/observability/amp

          AMP_ENDPOINT=$(terragrunt output -raw workspace_endpoint 2>/dev/null || echo "")
          if [[ -z "$AMP_ENDPOINT" ]]; then
            echo "::error::Failed to get AMP workspace endpoint. Ensure AMP is deployed."
            exit 1
          fi

          ROLE_ARN=$(terragrunt output -raw prometheus_agent_role_arn 2>/dev/null || echo "")
          if [[ -z "$ROLE_ARN" ]]; then
            echo "::error::Failed to get Prometheus Agent IAM role ARN. Ensure AMP is deployed."
            exit 1
          fi

          echo "amp-endpoint=${AMP_ENDPOINT}" >> $GITHUB_OUTPUT
          echo "role-arn=${ROLE_ARN}" >> $GITHUB_OUTPUT

      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig \
            --name tekmetric-qa \
            --region us-east-1

      - name: Prepare Helm Values
        run: |
          cd sre/helm/observability

          cat > values-qa-generated.yaml << EOF
          global:
            environment: qa
            region: us-east-1

          prometheusAgent:
            enabled: true
            replicas: 1

            serviceAccount:
              create: true
              name: prometheus-agent
              annotations:
                eks.amazonaws.com/role-arn: "${{ steps.terraform-outputs.outputs.role-arn }}"

            amp:
              remoteWriteUrl: "${{ steps.terraform-outputs.outputs.amp-endpoint }}"
              sigv4:
                enabled: true
                region: us-east-1

            scrapeInterval: 30s
            scrapeTimeout: 10s

            resources:
              requests:
                memory: "512Mi"
                cpu: "250m"
              limits:
                memory: "1Gi"
                cpu: "500m"

          kubeStateMetrics:
            enabled: true
            resources:
              requests:
                memory: "128Mi"
                cpu: "100m"
              limits:
                memory: "256Mi"
                cpu: "200m"

          nodeExporter:
            enabled: true
            resources:
              requests:
                memory: "64Mi"
                cpu: "50m"
              limits:
                memory: "128Mi"
                cpu: "100m"
          EOF

      - name: Ensure Namespace Exists
        if: github.event.inputs.action != 'uninstall'
        run: |
          kubectl create namespace observability --dry-run=client -o yaml | kubectl apply -f -
          echo "✅ Namespace 'observability' is ready"

      - name: Deploy/Upgrade Observability Stack
        if: github.event.inputs.action != 'uninstall'
        run: |
          cd sre/helm/observability

          helm upgrade --install observability . \
            --namespace observability \
            --values values-qa-generated.yaml \
            --wait \
            --timeout 10m

      - name: Uninstall Observability Stack
        if: github.event.inputs.action == 'uninstall'
        run: |
          helm uninstall observability --namespace observability --wait || true

      - name: Verify Deployment
        if: github.event.inputs.action != 'uninstall'
        run: |
          kubectl get pods -n observability
          kubectl wait --for=condition=ready pod \
            --all \
            --namespace observability \
            --timeout=300s

  # Deploy to PROD
  deploy-prod:
    name: Deploy to PROD
    runs-on: ubuntu-latest
    needs: determine-environments
    if: contains(fromJson(needs.determine-environments.outputs.environments), 'prod')
    environment:
      name: prod
      url: https://g-prod-grafana.grafana-workspace.us-east-1.amazonaws.com
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform and Terragrunt
        uses: ./.github/actions/terraform-setup

      - name: Assume AWS Role
        uses: ./.github/actions/aws-assume-role
        with:
          environment: prod
          role-arn: ${{ secrets.AWS_PROD_ROLE_ARN }}
          account-id: ${{ secrets.AWS_PROD_ACCOUNT_ID }}

      - name: Get Terraform Outputs
        id: terraform-outputs
        run: |
          cd sre/terragrunt/environments/prod/observability/amp

          AMP_ENDPOINT=$(terragrunt output -raw workspace_endpoint 2>/dev/null || echo "")
          if [[ -z "$AMP_ENDPOINT" ]]; then
            echo "::error::Failed to get AMP workspace endpoint. Ensure AMP is deployed."
            exit 1
          fi

          ROLE_ARN=$(terragrunt output -raw prometheus_agent_role_arn 2>/dev/null || echo "")
          if [[ -z "$ROLE_ARN" ]]; then
            echo "::error::Failed to get Prometheus Agent IAM role ARN. Ensure AMP is deployed."
            exit 1
          fi

          echo "amp-endpoint=${AMP_ENDPOINT}" >> $GITHUB_OUTPUT
          echo "role-arn=${ROLE_ARN}" >> $GITHUB_OUTPUT

      - name: Configure kubectl
        run: |
          aws eks update-kubeconfig \
            --name tekmetric-prod \
            --region us-east-1

      - name: Prepare Helm Values
        run: |
          cd sre/helm/observability

          cat > values-prod-generated.yaml << EOF
          global:
            environment: prod
            region: us-east-1

          prometheusAgent:
            enabled: true
            replicas: 2  # HA for production

            serviceAccount:
              create: true
              name: prometheus-agent
              annotations:
                eks.amazonaws.com/role-arn: "${{ steps.terraform-outputs.outputs.role-arn }}"

            amp:
              remoteWriteUrl: "${{ steps.terraform-outputs.outputs.amp-endpoint }}"
              sigv4:
                enabled: true
                region: us-east-1

            scrapeInterval: 15s  # More frequent for prod
            scrapeTimeout: 10s

            resources:
              requests:
                memory: "1Gi"
                cpu: "500m"
              limits:
                memory: "2Gi"
                cpu: "1000m"

          kubeStateMetrics:
            enabled: true
            resources:
              requests:
                memory: "256Mi"
                cpu: "200m"
              limits:
                memory: "512Mi"
                cpu: "400m"

          nodeExporter:
            enabled: true
            resources:
              requests:
                memory: "128Mi"
                cpu: "100m"
              limits:
                memory: "256Mi"
                cpu: "200m"
          EOF

      - name: Deploy/Upgrade Observability Stack
        if: github.event.inputs.action != 'uninstall'
        run: |
          cd sre/helm/observability

          helm upgrade --install observability . \
            --namespace observability \
            --create-namespace \
            --values values-prod-generated.yaml \
            --wait \
            --timeout 10m

      - name: Uninstall Observability Stack
        if: github.event.inputs.action == 'uninstall'
        run: |
          helm uninstall observability --namespace observability --wait || true

      - name: Verify Deployment
        if: github.event.inputs.action != 'uninstall'
        run: |
          kubectl get pods -n observability
          kubectl wait --for=condition=ready pod \
            --all \
            --namespace observability \
            --timeout=300s
